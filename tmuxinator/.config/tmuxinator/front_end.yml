# /Users/john/.config/tmuxinator/rails_console.yml

name: front_end
root: ~/development/front-end/

windows:
  - neovim:
      - nvim .
  - server:
      - nvim +":term npm run start-local 2>&1 | tee /tmp/angular-dev-server.log"
  - lazy_git:
      - lazygit
